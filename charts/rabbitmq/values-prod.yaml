replicaCount: 3

resources:
  requests:
    cpu: "250m"
    memory: "1Gi"
  limits:
    cpu: "500m"
    memory: "1.5Gi"

persistence:
  size: 20Gi

config:
  memoryHighWatermark: 900MiB
  diskFreeLimit: 2GiB

pdb:
  enabled: true
  minAvailable: "66%"
