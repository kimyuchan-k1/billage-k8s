apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: billage-apps
  namespace: billage-ops
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        generators:
          - list:
              elements:
                - chart: spring-boot
                  service: api
                  targetNamespace: billage-app
                - chart: nextjs
                  service: web
                  targetNamespace: billage-app
                - chart: fastapi
                  service: ai
                  targetNamespace: billage-app
                - chart: rabbitmq
                  service: mq
                  targetNamespace: billage-data
                - chart: qdrant
                  service: vectordb
                  targetNamespace: billage-data
          - list:
              elements:
                - env: dev
                  branch: develop
                  autoSync: true
                - env: prod
                  branch: main
                  autoSync: false

  template:
    metadata:
      name: 'billage-{{.service}}-{{.env}}'
      namespace: billage-ops
      labels:
        app.kubernetes.io/part-of: billage
        environment: '{{.env}}'
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: billage
      source:
        repoURL: 'https://github.com/kimyuchan-k1/billage-k8s.git'
        targetRevision: '{{.branch}}'
        path: 'charts/{{.chart}}'
        helm:
          valueFiles:
            - 'values.yaml'
            - 'values-{{.env}}.yaml'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{.targetNamespace}}'
      syncPolicy:
        automated:
          prune: '{{.autoSync}}'
          selfHeal: '{{.autoSync}}'
        syncOptions:
          - CreateNamespace=false
          - PruneLast=true
          - ApplyOutOfSyncOnly=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
      revisionHistoryLimit: 5
