apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: billage-infra
  namespace: billage-ops
  labels:
    app.kubernetes.io/part-of: billage
    component: infrastructure
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: billage
  source:
    repoURL: 'https://github.com/kimyuchan-k1/billage-k8s.git'
    targetRevision: main
    path: base
    directory:
      recurse: true
      include: '*.yaml'
      exclude: '*-example.yaml'
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: false  # 인프라는 수동 prune
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
  revisionHistoryLimit: 10
